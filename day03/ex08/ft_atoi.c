// Reproduce the behavior of the function atoi (man atoi).

#include <stdio.h>

int ft_atoi(char *str)
{
    int negative;
    int value;

    negative = 0;

    return (value);
}


int main()
{
    char *str;

    str = "Hello World";
    ft_atoi(str);
}
